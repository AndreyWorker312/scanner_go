. {
    # Локальные записи — проверяем первыми
    hosts {
        {$HOST_IP} scanner-vniitf.ru
        {$HOST_IP} www.scanner-vniitf.ru
        fallthrough
    }
    # Всё остальное — форвардим на внешние DNS
    forward . 8.8.8.8 1.1.1.1
    cache 300
    log
    errors
}

